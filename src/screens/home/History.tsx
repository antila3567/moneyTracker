import React from 'react';
import { View, Text, TouchableOpacity } from 'react-native';

const History = ({}) => {
  return (
    <TouchableOpacity>
      <Text>History</Text>
    </TouchableOpacity>
  );
};

export default History;
