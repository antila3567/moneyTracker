import React from 'react';
import { View, Text, TouchableOpacity } from 'react-native';

const Wallet = ({}) => {
  return (
    <TouchableOpacity>
      <Text>Wallet</Text>
    </TouchableOpacity>
  );
};

export default Wallet;
